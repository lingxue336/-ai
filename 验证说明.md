# 本机验证记录

验证时间：2026-09-20 至 2026-09-22。运行环境：Windows、Java 21、RTX 5080 Laptop GPU、约 32 GB 内存。

## 已实际执行

- 本机下载并调用 qwen3-vl:4b，识别带错误作答的测试照片，准确区分“判别式计算正确”与“根的情况判断错误”。这只是一个定向用例，不代表总体准确率。
- 真实下载一条 B 站初中教学视频（BV18x411x79x），保留原作者与链接。
- 通过 Whisper.cpp small 模型转写完整约 196 秒视频，得到 68 条带时间戳字幕。已修复中文目录兼容及字幕尾部超时长问题。
- 通过 FFmpeg 把 53.5–73.8 秒剪成新 MP4；实际时长 20.32322 秒。
- 浏览器真实操作：上传图片 → 本机分析 → 确认卡点 → 内容匹配 → 推荐上述 20 秒短片。
- Chrome 成功解码并播放该短片；播放地址属于 `/api/v2/clips/.../play`，不是原视频接口。
- 播放接口支持 HTTP Range，测试读取 0–1023 字节返回 206 及 1024 字节。
- 桌面 1440 像素和手机 390 像素宽度检查；无横向溢出和脚本异常。
- 旧推荐延迟返回期间更改问题，不会覆盖新的题目状态。

## 自动化测试覆盖

本轮执行 15 项 Java 自动化测试，全部通过。另运行真实本地模型和 Chrome 浏览器端到端检查（不是模型 mock）。

- SRT、B 站 JSON 字幕解析，非法时间范围拒绝。
- B 站 URL 域名和格式检查，拒绝本地/伪造域名链接。
- 真正生成红蓝两段测试视频，从第 6 秒剪辑，核对首帧确为蓝色与短片时长，防止只是原片跳转。
- 未审核、越界片段不可发布；不存在的候选 ID 不被模型“编造”为推荐。
- 未覆盖卡点返回空结果，不用整课兜底；照片不可读时不可推荐。
- 模型请求确实携带图像；未知知识卡点被过滤；数据重载保留真实短片。
- 本地模型结构化返回兼容性及交互任务与后台任务隔离回归测试。
- 实际本地模型反向用例：Δ＝0 错题不接收仅覆盖 Δ＜0 的短片；无作答照片不编造作答；英语图片被判为非数学，不进入推荐。
- 实际视频画面六宫格与字幕联合生成打点建议；返回的建议仍未自动发布，起止与标签须老师复核。
- 2026-09-22：上传后隐藏上传框、照片原位展示；大图拖动选框、应用、取消、还原和手机宽度检查通过。
- B 站公开搜索页条目解析去重、来源域名验证及验证码页面不冒充结果的回归测试通过。

## 联网补齐与剪辑边界实测（2026-09-22）

- 使用照片中的配方法符号错误，确认 `quadratic-completing-square`；原库无对应短片时，从页面提交“初中数学 配方法 完全平方公式”。
- 真实公开搜索、下载 B 站候选、本地 Whisper 转写均已执行；再次处理同一来源会复用已保存的原片和字幕。
- 成功从《数学重要方法“配方法”完全讲解，还不赶紧学？》（原长约 1069.87 秒，作者：大数学魔法师寻道）截取 79.24–276.92 秒；独立 MP4 实测 197.721334 秒。包含例题、一次项系数减半、负号和完成配方的结论。
- 浏览器端成功接收联网任务结果、解码并播放 `/api/v2/clips/6328a284-84dc-4f49-9f43-c525b5002bf3/play`，显示“未经老师人工审核”。截图：`target/verification/online-generated-clip.png`。
- 发现并修复“模型概括了全视频，但起止选在实际讲解之前”的错误：现在只用选定区间自身字幕与抽帧再次复核，可否决并重选一次；两次失败则不发布。新增回归测试验证复核不读取范围外字幕。
- 早期 15.62 秒测试片 `267b95d4-dc97-4414-854b-05e5d5013e6c` 未包含负号核心讲解，已从推荐中隔离；文件和原始记录保留用于排查。旧 `automatic` 状态不再进入推荐，新自动片段为 `automatic-boundary-checked`。
- 以上只是具体用例验证，不是总体准确率证明。边界复核仍为模型判断，不能替代教师审阅。

## 尚不能据此声称

未做真实学生的大样本准确率测试；未建立覆盖初一至初三的完整视频库；未验证所有 B 站链接都能导入；未实现面向公网的多用户权限体系。自动字幕和打点建议仍需教师复核，只有题干而没有作答时，不能据此确定学生的真实薄弱点。
